# Suppress funding messages
fund=false

# Note: node-domexception deprecation warning is from transitive dependency
# (supabase → node-fetch → fetch-blob → node-domexception)
# Safe to ignore - Node.js 18+ has native DOMException support
# Will be resolved when upstream packages update

